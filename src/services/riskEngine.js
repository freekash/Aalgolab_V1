// placeholder risk engine logic
module.exports = {
  check(order, riskSettings) {
    // return { allowed: true, message: '' } or { allowed:false, message:'reason' }
    return { allowed: true, message: '' };
  }
};